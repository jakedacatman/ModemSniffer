{
	--channel = , --uncomment this line and change the channel value to what the external modem broadcasts on
	blacklist = { }, --add channels you'd wish to blacklist here
	origin = "", --is there an origin variable in the message? if so, filter by just that origin here
	truncate = nil, --wanna cut messages short? put the number of chars to have
	delay = nil, --want to limit when new messages are received? set this to the delay
}
